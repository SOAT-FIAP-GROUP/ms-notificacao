apiVersion: apps/v1
kind: Deployment
metadata:
  name: notificacao-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notificacao-app
  template:
    metadata:
      labels:
        app: notificacao-app
    spec:
      containers:
        - name: notificacao-container
          image: joseaugustodossantos/notificacao-app
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: SERVICE_PEDIDO
              valueFrom:
                configMapKeyRef:
                  name: notificacao-config
                  key: services.pedido.url
            - name: SERVICE_PRODUCAO
              valueFrom:
                configMapKeyRef:
                  name: notificacao-config
                  key: services.producao.url
            - name: SPRING_PROFILES_ACTIVE
              valueFrom:
                configMapKeyRef:
                  name: notificacao-config
                  key: spring.profiles.active
          resources:
            requests:
              cpu: "100m"
              memory: 200Mi
            limits:
              cpu: "300m"
              memory: 400Mi
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 120
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 120
            periodSeconds: 5